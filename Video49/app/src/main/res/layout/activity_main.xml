<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    android:id="@+id/MainActivity"
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".MainActivity"
    android:orientation="vertical"
    android:padding="16dp">

    <EditText
        android:id="@+id/MainActivity_EditText_Username"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Username">
    </EditText>

    <EditText
        android:id="@+id/MainActivity_EditText_Password"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Password"
        android:layout_marginBottom="50dp"
        android:inputType="textPassword">
    </EditText>

    <Button
        android:id="@+id/MainActivity_Button_Guardar"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Guardar"
        android:onClick="onClick">
    </Button>

    <Button
        android:id="@+id/MainActivity_Button_Cargar"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Cargar"
        android:onClick="onClick">
    </Button>

    <TextView
        android:id="@+id/MainActivity_TextView_Username"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        tools:text="Kamarov"
        android:textSize="20sp">
    </TextView>

    <TextView
        android:id="@+id/MainActivity_TextView_Password"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        tools:text="12345"
        android:textSize="20sp">
    </TextView>


</LinearLayout>